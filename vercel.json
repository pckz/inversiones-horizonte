{
  "installCommand": "npm ci && cd backend && npm ci",
  "buildCommand": "cd backend && npx prisma generate && npm run build && cd .. && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/:path*", "destination": "/index.html" }
  ]
}
